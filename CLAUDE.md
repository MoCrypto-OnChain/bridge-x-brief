# Bridge X Brief — Agent Reference

This file is a pointer only. It does not contain project state, spec content, or a summary of either — see the canonical file placement convention below.

**Canonical source of truth:** [`docs/bridge-x-brief/PROJECT-CONTINUITY.md`](docs/bridge-x-brief/PROJECT-CONTINUITY.md). Read that file directly before doing any work in this repo — it holds current state, approved decisions, open items, and this repo's cross-repo role as a source fixture for other pipelines.

## Canonical file placement convention

This repo follows the Bridge X Capital stack-wide convention (see the root `CLAUDE.md` at the top of the `dev/` tree): tool-read files (`CLAUDE.md`, `AGENTS.md`) stay at the repo root but hold only a pointer — never spec/contract content or a paraphrase of it. This file deliberately never names a specific spec version.
